import React from 'react';
import { Header } from 'Components/Header';

const App: React.FC = () => {
    return (
        <div>
            <Header title="MERN app - sport tracker" />
        </div>
    );
};

export default App;
